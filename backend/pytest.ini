[tool:pytest]
testpaths = backend/tests
python_files = test_*.py
addopts =
    --verbose
    --tb=short
    --cov=backend/app
    --cov-report=term-missing
markers =
    unit: Unit tests
    external_api: Tests requiring external API access (may be slow)
